export interface Carousel {
    id: number;
    user_id: number;
    description: string;
    status: string;
    image: string;
    createdAt: string;
    updatedAt: string;
}

export interface Responses {
    error: string;
}